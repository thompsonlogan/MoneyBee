using Microsoft.VisualStudio.TestTools.UnitTesting;
using MoneyBee;

namespace RegistrationManagerTests
{
    [TestClass]
    public class UnitTest1
    {
        /*[TestMethod]
        public void usernameExistsTest()
        {
            //Arrange
            RegistrationManager regManager = new RegistrationManager();
            bool expected = true;

            //Actual
            bool actual = regManager.usernameExists("admin");

            //Assert
            Assert.AreEqual(expected, actual, "Positive - Name matches expected");
        }
        [TestMethod]
        public void registerUserTest()
        {
            //Arrange
            RegistrationManager regManager = new RegistrationManager();
            regManager.registerUser("logan", "logan", "logan", "logan@gmail.com", 20000);
            bool expected = true;

            //Actual
            bool actual = regManager.usernameExists("logan");

            //Assert
            Assert.AreEqual(expected, actual, "Positive - Name matches expected");
        }*/
    }
}
